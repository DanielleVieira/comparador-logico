export const lessThan = (term1, term2) => term1 < term2;

export const greaterThan = (term1, term2) => term1 > term2;

export const lessOrEquals = (term1, term2) => term1 <= term2;

export const greaterOrEquals = (term1, term2) => term1 >= term2;

export const Equals = (term1, term2) => term1 === term2;

export const NotEquals = (term1, term2) => term1 !== term2;
